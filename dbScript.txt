# Script for new table orders_sim. Used to simulator exchange

CREATE TABLE `orders_sim` (
  `uuid` varchar(45) NOT NULL,
  `market_name` varchar(32) DEFAULT NULL,
  `quantity` double DEFAULT NULL,
  `price` double DEFAULT NULL,
  `date_time` datetime DEFAULT NULL,
  `date` varchar(10) DEFAULT NULL,
  `time` varchar(10) DEFAULT NULL,
  `order_type` varchar(5) DEFAULT NULL,
  PRIMARY KEY (`uuid`)
) ENGINE=InnoDB DEFAULT CHARSET=latin1;


#Index Script
create index date_create_index on my_data (date_create)

#create table analytics
CREATE TABLE `analytics` (
  `market_name` varchar(20) DEFAULT NULL,
  `bolinger_h` double DEFAULT NULL,
  `bolinger_m` double DEFAULT NULL,
  `bolinger_l` double DEFAULT NULL,
  `adx` double DEFAULT NULL,
  `p_n_l` double DEFAULT NULL,
  `portfolio_current_value` double DEFAULT NULL,
  `portfolio_previous_value` double DEFAULT NULL,
  `btc_current_amount` double DEFAULT NULL,
  `fluctuation_percent` double DEFAULT NULL,
  `transaction_count` int(11) NOT NULL,
  `transaction_type` varchar(10) DEFAULT NULL,
  `quantity` double DEFAULT NULL,
  `unit_price` double DEFAULT NULL,
  `transaction_total_price` double DEFAULT NULL,
  `transaction_datetime` datetime DEFAULT NULL,
  `transaction_date` varchar(10) DEFAULT NULL,
  `transaction_time` varchar(10) DEFAULT NULL,
  `time_process_started` varchar(30) DEFAULT NULL,
  `time_transaction_processed` varchar(30) NOT NULL,
  `rsi_value` double DEFAULT NULL,
  PRIMARY KEY (`transaction_count`,`time_transaction_processed`)
) ENGINE=InnoDB DEFAULT CHARSET=latin1 COMMENT='	';



